{
 "name": "contest-worker",
 "type": "module",
 "private": true,
 "scripts": {
  "setup:test": "node scripts/setup-test-data.mjs",
  "test": "npm run setup:test && node --test test/*.test.js",
  "dev": "wrangler dev",
  "deploy": "wrangler deploy",
  "dev:local": "npm run setup:test && node scripts/dev-local.mjs"
 },
 "devDependencies": {
  "wrangler": "^3"
 }
}
